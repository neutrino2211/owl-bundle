//import { listeners, emit } from '//tbranyen.com/events';
//import { html, use } from 'diffhtml';

const state = {
  activeComponent: null,
};

function useState(initialState) {
  if (state.activeComponent)
  return [initialState, newState => {

  }];
}

function MyComponent({ startCount }) {
  const { counter, setCounter } = useState(startCount);

  //return html`
  //  <div>${counter}</div>
  //  <button onClick=${() => setCounter(++counter)}>Increment count</button>
  //`;
}

state.activeComponent = MyComponent;
console.log(MyComponent({ startCount: 0 }));

//innerHTML(document.body, html`
//  <${MyComponent} start-count=${5} />
//`);
